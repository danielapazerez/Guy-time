import Foundation

enum SharedConfiguration {
    // Must match the App Group enabled for both the app and widget targets.
    static let appGroupIdentifier = "group.com.pazgroup.GuyTime"
    static let widgetSnapshotKey = "GuyTime.widget.snapshot.v2"
    static let cloudRootRecordName = "guy-time-family-root"
}
